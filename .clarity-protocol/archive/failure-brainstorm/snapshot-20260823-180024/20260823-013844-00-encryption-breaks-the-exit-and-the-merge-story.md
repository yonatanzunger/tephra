# Encryption breaks the exit and the merge story

**Source:** conversation

Encrypted blobs do not diff or merge, so append-union reconciliation for day files stops working and divergence becomes all-or-nothing per file. The files also stop being readable without the tool plus the key, which is R26's exit and the departer's whole position.

## Additional Context

Structurally the same fork as Q2's CRDT-versus-plain-files, which resolved against the whole-corpus answer. Recording it so the next cycle argues it rather than rediscovering it.
